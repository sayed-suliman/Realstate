<!-- jQuery -->
<script src="/dashboard/plugins/jquery/jquery.min.js"></script>
<!-- Bootstrap -->
<script src="/dashboard/plugins/bootstrap/js/bootstrap.bundle.min.js"></script>

<!-- OPTIONAL SCRIPTS -->
<script src="/dashboard/plugins/chart.js/Chart.min.js"></script>
<!-- AdminLTE -->
<script src="/dashboard/dist/js/adminlte.js"></script>
<!-- AdminLTE for demo purposes -->
<script src="/dashboard/dist/js/demo.js"></script>

<!-- AdminLTE dashboard demo (This is only for demo purposes) -->
{{!--
<script src="/dashboard/dist/js/pages/dashboard3.js"></script> --}}

<script>
    $(document).ready(function () {
        const url = $(location).attr('href')
        // dashboard
        if (url.includes("dashboard")) {
            $("#dashboard-link").addClass("active")
        }
        //table
        if (url.includes("table")) {
            $("#dashboard-link").removeClass("active")
            $("#table-link").addClass("active")
        }
        //calendar
        if (url.includes("calendar")) {
            $("#dashboard-link").removeClass("active")
            $("#calendar-link").addClass("active")
        }

        // mailbox=>
        //read
        if (url.includes("read")) {
            $("#dashboard-link").removeClass("active")
            $('#mailbox-link').addClass("active")
            $("#read-link").addClass("active")
        }
        //inbox
        if (url.includes("inbox")) {
            $("#dashboard-link").removeClass("active")
            $('#mailbox-link').addClass("active")
            $("#inbox-link").addClass("active")
        }
        //compose
        if (url.includes("compose")) {
            $("#dashboard-link").removeClass("active")
            $('#mailbox-link').addClass("active")
            $("#compose-link").addClass("active")
        }

        // pages or examples =>
        //invoice
        if (url.includes("invoice")) {
            $("#dashboard-link").removeClass("active")
            $('#page-link').addClass("active")
            $("#invoice-link").addClass("active")
        }
        //invoice
        if (url.includes("invoice")) {
            $("#dashboard-link").removeClass("active")
            $('#page-link').addClass("active")
            $("#invoice-link").addClass("active")
        }
        //profile
        if (url.includes("profile")) {
            $("#dashboard-link").removeClass("active")
            $('#page-link').addClass("active")
            $("#profile-link").addClass("active")
        }
        //add-package
        if (url.includes("add-package")) {
            $("#dashboard-link").removeClass("active")
            $('#package-link').addClass("active")
            $("#package-add-link").addClass("active")
        }
        //package-detail
        if (url.includes("package-detail")) {
            $("#dashboard-link").removeClass("active")
            $('#package-link').addClass("active")
            $("#package-detail-link").addClass("active")
        }
        //courses(student view)
        if (url.includes("courses")) {
            $("#dashboard-link").removeClass("active")
            $('#courses').addClass("active")
        }
        //courses(student view)
        if (url.includes("course")) {
            $("#dashboard-link").removeClass("active")
            $('#courses').addClass("active")
        }
        //add-course
        if (url.includes("add-course")) {
            $("#dashboard-link").removeClass("active")
            $('#course-link').addClass("active")
            $("#course-add-link").addClass("active")
        }
        //course-detail
        if (url.includes("course-detail")) {
            $("#dashboard-link").removeClass("active")
            $('#course-link').addClass("active")
            $("#course-detail-link").addClass("active")
        }
        //add-chapter
        if (url.includes("add-chapter")) {
            $("#dashboard-link").removeClass("active")
            $('#chapter-link').addClass("active")
            $("#chapter-add-link").addClass("active")
        }
        //chapter-detail
        if (url.includes("chapter-detail")) {
            $("#dashboard-link").removeClass("active")
            $('#chapter-link').addClass("active")
            $("#chapter-detail-link").addClass("active")
        }
        //add-quiz
        if (url.includes("add-quiz")) {
            $("#dashboard-link").removeClass("active")
            $('#quiz-link').addClass("active")
            $("#quiz-add-link").addClass("active")
        }
        //quiz-detail
        if (url.includes("quiz-detail")) {
            $("#dashboard-link").removeClass("active")
            $('#quiz-link').addClass("active")
            $("#quiz-detail-link").addClass("active")
        }
        //add-voucher
        if (url.includes("add-coupon")) {
            $("#dashboard-link").removeClass("active")
            $('#voucher-link').addClass("active")
            $("#voucher-add-link").addClass("active")
        }
        //voucher-detail
        if (url.includes("coupon-detail")) {
            $("#dashboard-link").removeClass("active")
            $('#voucher-link').addClass("active")
            $("#voucher-detail-link").addClass("active")
        }
        //add users
        if (url.includes("add-user")) {
            $("#dashboard-link").removeClass("active")
            $('#users-link').addClass("active")
            $("#users-add-link").addClass("active")
        }
        // user-edit
        if (url.includes("user-edit")) {
            $("#dashboard-link").removeClass("active")
            $('#users-link').addClass("active")
            $("#users-all-link").addClass("active")
        }
        if (url.includes("users")) {
            $("#dashboard-link").removeClass("active")
            $('#users-link').addClass("active")
            $("#users-all-link").addClass("active")
        }
        //sort
        if (url.includes("sort")) {
            $("#dashboard-link").removeClass("active")
            // $('#page-link').addClass("active")
            $("#sort-link").addClass("active")
        }
        //contact-us
        if (url.includes("contact-us")) {
            $("#dashboard-link").removeClass("active")
            // $('#page-link').addClass("active")
            $("#contact-us-link").addClass("active")
            $("#contact-link").removeClass("active")
        }

        //order
        if (url.includes("order")) {
            $("#dashboard-link").removeClass("active")
            $('#order-link').addClass("active")
        }
        //message
        if (url.includes("messages")) {
            $("#dashboard-link").removeClass("active")
            $('#messages-link').addClass("active")
        }
        //message
        if (url.includes("read-message")) {
            $("#dashboard-link").removeClass("active")
            $('#messages-link').addClass("active")
        }
        //setting
        if (url.includes("setting")) {
            $("#dashboard-link").removeClass("active")
            $('#setting-link').addClass("active")
        }

        //salesperson -- category
        if (url.includes("add-category")) {
            $("#dashboard-link").removeClass("active")
            $('#category').addClass("active")
            $('#category-add').addClass("active")
        }
        //salesperson -- category
        if (url.includes("all-category")) {
            $("#dashboard-link").removeClass("active")
            $('#category').addClass("active")
            $('#category-detail').addClass("active")
        }
        //salesperson -- quiz-result
        if (url.includes("result-by-category")) {
            $("#dashboard-link").removeClass("active")
            $('#quiz').addClass("active")
            $('#quiz-result').addClass("active")
        }
        //salesperson -- quiz-category
        if (url.includes("test-by-category")) {
            $("#dashboard-link").removeClass("active")
            $('#quiz').addClass("active")
            $('#quiz-category').addClass("active")
        }
        //salesperson -- test
        if (url.includes("tests")) {
            $("#dashboard-link").removeClass("active")
            $('#tests').addClass("active")
            $('#tests-all').addClass("active")
        }
        //salesperson -- test-result
        if (url.includes("tests-result")) {
            $("#dashboard-link").removeClass("active")
            $('#tests').addClass("active")
            $('#tests-result').addClass("active")
            $('#tests-all').removeClass("active")
        }
        //salesperson -- exam
        if (url.includes("exam")) {
            $("#dashboard-link").removeClass("active")
            $('#exam').addClass("active")
            $('#exam-replica').addClass("active")
        }
        //salesperson -- exam-result
        if (url.includes("exam-result")) {
            $("#dashboard-link").removeClass("active")
            $('#exam').addClass("active")
            $('#exam-result').addClass("active")
            $('#exam-replica').removeClass("active")
        }

        $(".nav-item").click(function () {
            if ($(this).attr("class").includes('menu-is-opening menu-open')) {
                $(this).attr("class", "nav-item")
            }
        })

    })

    //showing alert(toast)
    {{#if toast}}
    $('.toast').toast('show')
    setTimeout(() => {
        $('.toast').toast('dispose')
    }, 5000)
    {{/if}}
    {{#if toast_error}}
    $('.toast').toast('show')
    setTimeout(() => {
        $('.toast').toast('dispose')
    }, 5000)
    {{/if}}
</script>